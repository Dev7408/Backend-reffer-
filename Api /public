<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TrendBot Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
        import { getFirestore, doc, getDoc } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";

        const firebaseConfig = {
            apiKey: "AIzaSyABn6qYXTvrffjuEhcDaEHU-ipAvb9YXMQ",
            authDomain: "refer-ffd56.firebaseapp.com",
            projectId: "refer-ffd56",
            storageBucket: "refer-ffd56.firebasestorage.app",
            appId: "1:781090144574:web:069215b2449a1791b22c6f"
        };

        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);

        window.showTab = (id) => {
            document.querySelectorAll('.tab-content').forEach(c => c.classList.add('hidden'));
            document.getElementById(id).classList.remove('hidden');
        };
    </script>
</head>
<body class="bg-slate-900 text-white pb-24 font-sans">
    <div id="home" class="tab-content p-6 text-center">
        <div class="bg-blue-600 p-10 rounded-3xl shadow-lg mb-6">
            <p class="text-sm opacity-80 uppercase">Available Coins</p>
            <h1 class="text-6xl font-black mt-2">💰 <span id="uCoins">0</span></h1>
        </div>
        <button class="w-full bg-yellow-500 text-black font-bold py-4 rounded-2xl mb-4">REFER FRIENDS (50 Coins)</button>
    </div>
    <nav class="fixed bottom-0 left-0 right-0 bg-slate-800 p-4 flex justify-around border-t border-slate-700">
        <button onclick="showTab('home')">🏠</button>
        <button onclick="showTab('tasks')">📋</button>
        <button onclick="showTab('withdraw')">💸</button>
        <button onclick="showTab('profile')">👤</button>
    </nav>
</body>
</html>
